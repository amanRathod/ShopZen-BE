# ===============================
# = DATA SOURCE
# ===============================
# Set here configurations for the database connection
spring.datasource.url=jdbc:postgresql://dpg-cgvq4g8dh87joktmvk3g-a.ohio-postgres.render.com/shopzen?user=aman&password=7dAjrlkXGxVV2zpy15nKhD6ErFfgdEhr
spring.datasource.driver-class-name:org.postgresql.Driver
# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================
# Show or not log for each sql query
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format-sql=true

# Hibernate ddl auto (create, create-drop, update): with "validate" the database
# Hibernate validates the schema against the entities
spring.jpa.hibernate.ddl-auto=update

spring.jpa.database=postgresql
# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# ===============================

# ===============================
# = SECURITY
# ===============================
spring.data.rest.base-path=/api
spring.data.rest.detection-strategy=annotated
allowed.origins=http://localhost:3000

# configure server
server.port=8080
server.error.include-message=always
logging.level.org.springframework.data.rest=DEBUG


# ===============================
# = JWT
# ===============================
spring.application.security.jwt.secret-key=6D5A7134743777217A25432A462D4A614E645267556B58703272357538782F41
# 1 day
spring.application.security.jwt.expiration=600000000
